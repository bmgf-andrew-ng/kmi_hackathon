# AWS Bedrock credentials (required)
AWS_ACCESS_KEY_ID=your-access-key-id
AWS_SECRET_ACCESS_KEY=your-secret-access-key
AWS_REGION=us-east-1

# Data store connection strings
# When running inside devcontainer, these are set via containerEnv — no need to set here.
# When running locally outside devcontainer, uncomment and set these:
# OPENSEARCH_URL=http://localhost:9200
# OPENSEARCH_USER=admin
# OPENSEARCH_PASSWORD=admin
# NEO4J_URI=bolt://localhost:7687
# NEO4J_USER=neo4j
# NEO4J_PASSWORD=password
# AZURE_STORAGE_BLOB_ENDPOINT=http://127.0.0.1:10000/devstoreaccount1
# AZURE_STORAGE_CONTAINER=strategy-pages

# Workspace root (auto-set in devcontainer, set manually if running elsewhere)
# WORKSPACE_ROOT=/workspaces/gf-hackathon
