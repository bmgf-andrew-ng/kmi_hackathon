# Neo4j
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=password

# OpenSearch
OPENSEARCH_URL=http://localhost:9200
OPENSEARCH_USER=admin
OPENSEARCH_PASSWORD=admin

# Azurite (Azure Blob Storage emulator)
# Account name and key are well-known Azurite constants (hardcoded in MCP server).
# Only the endpoint changes between local (localhost) and DevContainer (azurite).
AZURE_STORAGE_BLOB_ENDPOINT=http://127.0.0.1:10000/devstoreaccount1
AZURE_STORAGE_CONTAINER=strategy-pages
